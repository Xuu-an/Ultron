# Azure OpenAI 配置
# 从 Azure Portal 获取的 API 密钥
AZURE_OPENAI_API_KEY=your-azure-api-key-here

# Azure OpenAI 资源的终结点 URL
# 格式：https://<your-resource-name>.openai.azure.com/
AZURE_OPENAI_API_BASE=https://your-resource-name.openai.azure.com/

# API 版本，建议使用最新的稳定版本
AZURE_OPENAI_API_VERSION=2023-05-15

# 模型部署名称，在 Azure OpenAI Studio 中配置
# 注意：这是你在 Azure 中部署模型时设置的名称，而不是模型本身的名称
# 例如：你可能将 gpt-4 模型部署为 "gpt4-deployment"
AZURE_OPENAI_DEPLOYMENT_NAME=your-deployment-name

# 标准 OpenAI API 配置（如果需要使用标准 OpenAI 服务）
# 从 OpenAI 平台获取的 API 密钥
OPENAI_API_KEY=your-openai-api-key-here 